# Copy to .env and fill in. .env is gitignored and must never be committed.
# A real environment variable of the same name wins over the value here.

FIREWORKS_API_KEY=
TAVILY_API_KEY=
NOTION_TOKEN=
NOTION_DATABASE_ID=
